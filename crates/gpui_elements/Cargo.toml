[package]
name = "gpui_elements"
version = "0.1.0"
edition.workspace = true
publish.workspace = true
license = "Apache-2.0"

[lints]
workspace = true

[dependencies]
gpui.workspace = true

[dev-dependencies]
gpui = { path = "../gpui", features = ["test-support"] }
